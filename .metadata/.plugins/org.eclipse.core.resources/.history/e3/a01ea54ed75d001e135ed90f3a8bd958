package Lab1;

public class AgentProgram {

	public Action execute(Percept p) {// location, status
		if(p.getLocationState().equals("DIRTY")) {
			return p.getAgentLocation();
		}
		return NoOpAction.NO_OP;
		
	}
}